import SeekerAppShell from "@/components/seeker/SeekerAppShell";

export default function SeekerLayout({ children }: { children: React.ReactNode }) {
  return <SeekerAppShell>{children}</SeekerAppShell>;
}
